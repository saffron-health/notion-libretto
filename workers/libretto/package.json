{
  "name": "libretto",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "deploy": "node --env-file=../../.env ./scripts/deploy.mjs",
    "env:push": "node --env-file=../../.env ./scripts/env-push.mjs"
  },
  "dependencies": {
    "@notionhq/workers": "latest",
    "libretto": "^0.6.15"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
