LIBRETTO_API_KEY=
LIBRETTO_CALLBACK_URL=
LIBRETTO_CALLBACK_SECRET=

# Optional. If set, the worker requires this exact value in the
# `x-webhook-secret` header on every inbound request. Leave unset
# to rely only on the secret embedded in the deployed webhook URL.
WEBHOOK_SHARED_SECRET=
